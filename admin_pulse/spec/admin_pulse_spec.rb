require 'spec_helper'

describe AdminPulse do
  it 'has a version number' do
    expect(AdminPulse::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
